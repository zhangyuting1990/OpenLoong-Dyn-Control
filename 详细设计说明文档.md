# OpenLoong Dynamics Control 详细设计说明文档

## 1. 项目概述

### 1.1 项目简介
OpenLoong Dynamics Control是一套基于MPC（模型预测控制）与WBC（全身控制）的仿人机器人运动控制框架，基于Mujoco仿真平台开发，适用于"青龙"人形机器人模型。

### 1.2 项目目录结构

```
/workspace/
├── algorithm/          # 核心控制算法
├── assets/            # 资源文件（图片、动图）
├── common/            # 公共模块（数据总线、PVT控制、数据记录）
├── demo/              # 演示程序
├── doc/               # 文档（Doxygen、Sphinx）
├── math/              # 数学工具库
├── models/            # 机器人模型文件（URDF、XML）
├── record/            # 数据记录与分析脚本
├── sim_interface/     # Mujoco仿真接口
└── third_party/       # 第三方库（Eigen、Pinocchio、Mujoco、qpOASES等）
```

### 1.3 核心技术特点
- **分层模块化设计**：系统采用分层架构，控制框架结构清晰
- **数据总线通信**：应用DataBus进行模块间数据交互
- **MPC + WBC双层控制**：MPC负责高层运动规划，WBC负责底层力矩控制
- **基于Mujoco仿真**：支持高保真物理仿真

---

## 2. 接口依赖层次架构（从前到后）

### 2.1 整体数据流架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          用户输入层 (User Input)                              │
│                    键盘/摇杆 → 期望速度/姿态指令                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                       前端接口层 (Sim Interface)                            │
│  ┌──────────────────┐    ┌──────────────────┐    ┌──────────────────┐      │
│  │  MJ_Interface    │    │ GLFW_Callbacks   │    │   UI Controller  │      │
│  │  (Mujoco数据接口) │    │   (GLFW回调)      │    │   (UI控制)        │      │
│  └────────┬─────────┘    └──────────────────┘    └──────────────────┘      │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         数据总线层 (DataBus)                                  │
│                    RobotState: 全局状态数据容器                               │
│    ┌──────────────────────────────────────────────────────────────────────┐  │
│    │ 传感器数据 │ 关节状态 │ MPC结果 │ WBC结果 │ PVT结果 │ 指令数据       │  │
│    └──────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
            ┌───────────────────────┼───────────────────────┐
            ▼                       ▼                       ▼
┌───────────────────┐   ┌───────────────────┐   ┌───────────────────┐
│   指令解析层       │   │   规划层           │   │   控制层           │
│ JoyStickInterpreter│   │ GaitScheduler     │   │   PVT_Ctr         │
│ (摇杆指令解析)     │   │ FootPlacement      │   │   (关节PVT控制)    │
└───────────────────┘   │ MPC               │   └───────────────────┘
                        │ WBC_priority       │             │
                        └───────────────────┘             │
                                │                         │
                                ▼                         │
                        ┌───────────────────┐             │
                        │  Pin_KinDyn       │             │
                        │ (运动学/动力学)     │             │
                        └───────────────────┘             │
                                │                         │
                                └────────────┬────────────┘
                                             ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         物理仿真层 (Mujoco)                                  │
│                     机器人模型 + 物理仿真引擎                                 │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 各层详细说明

---

## 3. 前端接口层 (Sim Interface)

### 3.1 MJ_Interface - Mujoco数据接口

**文件位置**: `sim_interface/MJ_interface.h` / `MJ_interface.cpp`

**功能职责**:
- 作为Mujoco仿真器与控制程序之间的桥梁
- 从Mujoco读取传感器数据（关节位置、速度、力矩、基座姿态等）
- 向Mujoco写入电机力矩指令

**主要接口方法**:

| 方法名 | 功能 | 数据方向 |
|--------|------|----------|
| `MJ_Interface(mjModel*, mjData*)` | 构造函数，初始化接口 | 初始化 |
| `updateSensorValues()` | 从Mujoco读取所有传感器数据 | 输入 |
| `setMotorsTorque(vector<double>&)` | 向Mujoco写入电机力矩指令 | 输出 |
| `dataBusWrite(DataBus&)` | 将传感器数据写入数据总线 | 输出 |

**依赖的Mujoco传感器**:
- `baselink-quat`: 基座四元数姿态
- `baselink-velocity`: 基座线速度
- `baselink-gyro`: 基座角速度
- `baselink-baseAcc`: 基座加速度

**传感器数据结构**:
```cpp
double rpy[3];              // roll, pitch, yaw 姿态角
double f3d[3][2];           // 足端接触力 [x,y,z][L,R]
double basePos[3];          // 基座位置（世界坐标系）
double baseLinVel[3];       // 基座线速度（本体坐标系）
double baseAngVel[3];       // 基座角速度（本体坐标系）
double baseAcc[3];          // 基座加速度（本体坐标系）
vector<double> motor_pos;   // 关节位置
vector<double> motor_vel;   // 关节速度
```

**依赖关系**:
```
MJ_Interface → mujoco.h → DataBus
                    ↓
            直接写入 RobotState
```

---

## 4. 数据总线层 (DataBus)

### 4.1 DataBus - 全局状态容器

**文件位置**: `common/data_bus.h`

**功能职责**:
- 作为整个控制系统的数据中枢
- 所有模块间的数据交互通过DataBus完成
- 采用"读取-计算-写入"的简单逻辑

**核心数据结构分类**:

#### 4.1.1 传感器反馈数据
```cpp
double rpy[3];                    // 基座姿态角 (roll, pitch, yaw)
double fL[3], fR[3];             // 左右足端接触力
double basePos[3];               // 基座位置
double baseLinVel[3];            // 基座线速度
double baseAcc[3];               // 基座加速度
double baseAngVel[3];            // 基座角速度
vector<double> motors_pos_cur;   // 当前关节位置
vector<double> motors_vel_cur;   // 当前关节速度
vector<double> motors_tor_cur;   // 当前关节力矩
```

#### 4.1.2 关节控制指令
```cpp
vector<double> motors_pos_des;   // 期望关节位置
vector<double> motors_vel_des;   // 期望关节速度
vector<double> motors_tor_des;   // 期望关节力矩
vector<double> motors_tor_out;   // 最终输出力矩
```

#### 4.1.3 机器人状态变量
```cpp
Eigen::VectorXd q, dq, ddq;     // 广义坐标、速度、加速度
Eigen::MatrixXd J_base, J_l, J_r; // 雅可比矩阵（基座、左足、右足）
Eigen::MatrixXd dJ_base, dJ_l, dJ_r; // 雅可比矩阵导数
Eigen::Vector3d pCoM_W;          // 质心位置（世界坐标系）
Eigen::Matrix3d base_rot;        // 基座旋转矩阵
```

#### 4.1.4 动力学数据
```cpp
Eigen::MatrixXd dyn_M, dyn_M_inv;  // 质量矩阵及其逆
Eigen::MatrixXd dyn_C;             // 科里奥利矩阵
Eigen::VectorXd dyn_G;             // 重力向量
Eigen::MatrixXd dyn_Ag, dyn_dAg;   // 质心雅可比矩阵及其导数
Eigen::VectorXd dyn_Non;           // 非线性项 (C*dq + G)
```

#### 4.1.5 MPC相关数据
```cpp
Eigen::VectorXd Xd;               // MPC期望状态轨迹
Eigen::VectorXd X_cur;           // MPC当前状态
Eigen::VectorXd X_cal;           // MPC计算状态
Eigen::VectorXd dX_cal;          // MPC计算状态导数
Eigen::VectorXd fe_react_tau_cmd; // 足端反力矩指令
```

#### 4.1.6 WBC相关数据
```cpp
Eigen::Vector3d base_rpy_des;     // 期望基座姿态
Eigen::Vector3d base_pos_des;     // 期望基座位置
Eigen::Vector3d base_vel_des;     // 期望基座速度
Eigen::Vector3d base_omega_des;   // 期望基座角速度
Eigen::VectorXd wbc_delta_q_final; // WBC最终关节位置增量
Eigen::VectorXd wbc_dq_final;     // WBC最终关节速度
Eigen::VectorXd wbc_tauJointRes;  // WBC关节力矩结果
```

#### 4.1.7 步态相关数据
```cpp
Eigen::Vector3d swing_fe_pos_des_W;  // 摆动足足端期望位置（世界）
Eigen::Vector3d swing_fe_rpy_des_W;  // 摆动足足端期望姿态（世界）
Eigen::Vector3d stance_fe_pos_cur_W; // 支撑足足端当前位置（世界）
double tSwing;                       // 摆动时间
double phi;                          // 相位变量
enum LegState { LSt, RSt, DSt };    // 腿状态（左支撑、右支撑、双支撑）
enum MotionState { Stand, Walk, Walk2Stand }; // 运动状态
```

---

## 5. 指令解析层 (Command Interpretation)

### 5.1 JoyStickInterpreter - 摇杆指令解析

**文件位置**: `algorithm/joystick_interpreter.h` / `joystick_interpreter.cpp`

**功能职责**:
- 解析用户输入的期望速度/位置指令
- 生成平滑的轨迹（使用RampTrajectory）
- 支持世界坐标系和本体坐标系的速度指令

**主要接口**:

| 方法 | 功能 |
|------|------|
| `setVxDesLPara(double, double)` | 设置x方向期望速度（本体坐标）及到达时间 |
| `setVyDesLPara(double, double)` | 设置y方向期望速度（本体坐标）及到达时间 |
| `setWzDesLPara(double, double)` | 设置z方向期望角速度（本体坐标）及到达时间 |
| `setIniPos(...)` | 设置初始位置 |
| `step()` | 计算当前时刻的期望速度/位置 |
| `dataBusWrite(DataBus&)` | 写入数据总线 |

**输出数据**:
```cpp
double vx_W, vy_W, vz_W;  // 世界坐标系下期望线速度
double px_W, py_W, pz_W;  // 世界坐标系下期望位置
double vx_L, vy_L, wz_L;  // 本体坐标系下期望速度
```

**依赖关系**:
```
JoyStickInterpreter → RampTrajectory → DataBus
         │                              │
         └──────────写入────────────────┘
                   RobotState.js_*
```

---

## 6. 规划层 (Planning)

### 6.1 GaitScheduler - 步态调度器

**文件位置**: `algorithm/gait_scheduler.h` / `gait_scheduler.cpp`

**功能职责**:
- 管理双足行走周期
- 判断摆动腿和支撑腿
- 检测触地事件
- 提供步态相位信息

**核心参数**:

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `tSwing` | 单步摆动时长 | 0.4s |
| `FzThrehold` | 触地力阈值 | 100N |
| `phi` | 步态相位 (0~1) | - |
| `legState` | 当前支撑腿状态 | LSt/RSt |

**核心方法**:

| 方法 | 功能 |
|------|------|
| `step()` | 步态调度主计算 |
| `dataBusRead(DataBus&)` | 读取数据总线 |
| `dataBusWrite(DataBus&)` | 写入数据总线 |
| `start()` | 开始行走 |
| `stop()` | 停止行走 |

**状态机**:
```cpp
enum MotionState { Stand, Walk, Walk2Stand };
enum LegState { LSt, RSt, DSt };
```

**依赖关系**:
```
GaitScheduler → DataBus → Pin_KinDyn
                     ↑
         读取: RobotState.leg_contact, RobotState.FL_est, RobotState.FR_est
                     │
         写入: RobotState.legState, RobotState.motionState, RobotState.phi
```

---

### 6.2 FootPlacement - 落脚点规划

**文件位置**: `algorithm/foot_placement.h` / `foot_placement.cpp`

**功能职责**:
- 根据当前速度和姿态角速度计算落脚点
- 生成摆动腿的期望轨迹
- 支持跨障碍物的脚步规划

**核心参数**:

| 参数 | 说明 | 用途 |
|------|------|------|
| `kp_vx, kp_vy` | 落脚点x/y方向调节参数 | 根据速度调节落脚点 |
| `kp_wz` | 落脚点转向调节参数 | 根据角速度调节 |
| `stepHeight` | 抬腿高度 | 摆动轨迹高度 |
| `legLength` | 腿长 | 步长计算 |

**核心方法**:

| 方法 | 功能 |
|------|------|
| `getSwingPos()` | 计算摆动腿期望位置 |
| `Trajectory(double, double, double)` | 生成z方向摆动轨迹 |
| `dataBusRead(DataBus&)` | 读取数据 |
| `dataBusWrite(DataBus&)` | 写入数据 |

**轨迹生成**:
```cpp
// 摆动腿z方向轨迹（抛物线）
double FootPlacement::Trajectory(double phase, double des1, double des2);
// phase: 达到最高点的相位
// des1: 轨迹最高点位置
// des2: 轨迹最终位置
```

**依赖关系**:
```
FootPlacement → DataBus
       │
       ├──读取: RobotState.base_vel, RobotState.base_omega, RobotState.phi
       │
       └──写入: RobotState.swing_fe_pos_des_W, RobotState.swing_fe_rpy_des_W
```

---

## 7. 控制算法层 (Control Algorithms)

### 7.1 MPC - 模型预测控制

**文件位置**: `algorithm/mpc.h` / `mpc.cpp`

**功能职责**:
- 基于单刚体模型预测控制
- 优化足端接触力
- 跟踪期望的质心位置、速度和姿态

**控制模型**:
```
状态量 (12维): [eul(3), pCoM(3), omega(3), vCoM(3)]
控制量 (13维): [fL(3), tauL(3), fR(3), tauR(3), tauWaist(1)]
                或等效为 12维接触力 + 1维腰部力矩
```

**MPC参数定义**:
```cpp
const uint16_t nx = 12;           // 状态维度
const uint16_t nu = 13;            // 控制维度
const uint16_t mpc_N = 10;         // 预测步数
const uint16_t ch = 3;             // MPC控制间隔内的步数
```

**约束类型**:
1. **摩擦锥约束**: 限制足端力在摩擦锥内
2. **ZMP约束**: 保持质心在支撑多边形内
3. **力矩极限**: 各关节力矩限制

**核心方法**:

| 方法 | 功能 |
|------|------|
| `MPC(double dtIn)` | 构造函数 |
| `set_weight(...)` | 设置MPC权重矩阵 |
| `cal()` | 执行MPC计算 |
| `dataBusRead(DataBus&)` | 读取数据 |
| `dataBusWrite(DataBus&)` | 写入数据 |
| `enable()/disable()` | 使能/关闭MPC |

**权重参数说明**:
```cpp
// L_diag: 状态误差权重 [eul, pos, omega, vel]
// K_diag: 控制输入权重 [fl, tl, fr, tr]
void set_weight(double u_weight, Eigen::MatrixXd L_diag, Eigen::MatrixXd K_diag);
```

**依赖关系**:
```
MPC → DataBus → Pin_KinDyn
         │
         ├──读取: RobotState.X_cur, RobotState.base_rpy, RobotState.pCoM_W, ...
         │
         └──写入: RobotState.Xd, RobotState.fe_react_tau_cmd, RobotState.Fr_ff
```

---

### 7.2 Pin_KinDyn - 运动学与动力学求解器

**文件位置**: `algorithm/pino_kin_dyn.h` / `pino_kin_dyn.cpp`

**功能职责**:
- 基于Pinocchio库计算正/逆运动学
- 计算雅可比矩阵及其导数
- 计算动力学模型（质量矩阵、重力项、科里奥利项）
- 提供数值积分功能

**主要计算方法**:

| 方法 | 功能 |
|------|------|
| `computeJ_dJ()` | 计算所有关节的雅可比矩阵及导数 |
| `computeDyn()` | 计算动力学模型 (M, C, G, Non) |
| `computeInK_Leg(...)` | 腿部逆运动学求解 |
| `computeInK_Hand(...)` | 手臂逆运动学求解 |
| `dataBusRead(DataBus&)` | 读取数据 |
| `dataBusWrite(DataBus&)` | 写入数据 |

**输出的关键数据**:
```cpp
Eigen::MatrixXd dyn_M, dyn_M_inv;    // 质量矩阵及其逆
Eigen::MatrixXd dyn_C;               // 科里奥利矩阵
Eigen::VectorXd dyn_G;               // 重力向量
Eigen::VectorXd dyn_Non;            // 非线性项 (C*dq + g)
Eigen::Vector3d pCoM_W;             // 质心位置
Eigen::Matrix<double, 6, -1> J_l, J_r; // 左右足雅可比矩阵
```

**依赖的Pinocchio算法**:
```cpp
#include "pinocchio/algorithm/jacobian.hpp"    // 雅可比计算
#include "pinocchio/algorithm/kinematics.hpp"  // 运动学计算
#include "pinocchio/algorithm/frames.hpp"       // 末端雅可比
#include "pinocchio/algorithm/rnea.hpp"        // 逆动力学
#include "pinocchio/algorithm/crba.hpp"         // 质量矩阵
#include "pinocchio/algorithm/centroidal.hpp"  // 质心动力学
#include "pinocchio/algorithm/aba.hpp"         //  forward dynamics
```

**依赖关系**:
```
Pin_KinDyn → Pinocchio → DataBus
                        │
                        ├──读取: RobotState.q, RobotState.dq
                        │
                        └──写入: RobotState.J_*, RobotState.dyn_*, RobotState.pCoM_W
```

---

### 7.3 WBC_priority - 全身优先级控制

**文件位置**: `algorithm/wbc_priority.h` / `wbc_priority.cpp`

**功能职责**:
- 实现基于优先级的全身控制
- 通过QP求解器分配关节力矩
- 支持多任务层级（位置、姿态、接触力等）

**QP问题 formulation**:
```
min: ||Jc*ddq + dJc*dq - Fr||².Q1 + ||ddq||².Q2
s.t: M*ddq + C*dq + G = J^T*Fr + tauJoint
     摩擦锥约束
```

**默认任务优先级**（从高到低）:
```cpp
taskOrder.emplace_back("RedundantJoints");     // 冗余自由度
taskOrder.emplace_back("static_Contact");      // 静态接触
taskOrder.emplace_back("Roll_Pitch_Yaw_Pz");  // 姿态+高度
taskOrder.emplace_back("PxPy");               // 质心xy位置
taskOrder.emplace_back("SwingLeg");            // 摆动腿
taskOrder.emplace_back("HandTrack");           // 手部跟踪
```

**核心方法**:

| 方法 | 功能 |
|------|------|
| `computeDdq(Pin_KinDyn&)` | 计算期望关节加速度 |
| `computeTau()` | 计算关节力矩 |
| `dataBusRead(DataBus&)` | 读取数据 |
| `dataBusWrite(DataBus&)` | 写入数据 |
| `setQini(...)` | 设置初始姿态 |

**QP求解器参数**:
```cpp
static const int QP_nv_des = 18;   // QP变量数
static const int QP_nc_des = 22;   // QP约束数
qpOASES::QProblem QP_prob;          // QP求解器实例
```

**权重参数**:
```cpp
Eigen::MatrixXd Q1;  // 外部接触力误差权重
Eigen::MatrixXd Q2;  // 关节加速度权重
```

**依赖关系**:
```
WBC_priority → Pin_KinDyn → DataBus
                    │
                    ├──读取: RobotState.dyn_M, RobotState.J_*, ...
                    │
                    └──写入: RobotState.wbc_*, RobotState.tauJointCmd
```

---

### 7.4 PriorityTasks - 优先级任务管理

**文件位置**: `algorithm/priority_tasks.h` / `priority_tasks.cpp`

**功能职责**:
- 管理WBC中的多优先级任务
- 构建任务优先级链
- 计算各任务雅可比和误差

**Task结构**:
```cpp
struct Task {
    std::string taskName;     // 任务名称
    int id;                   // 任务ID
    int parentId, childId;    // 父子任务ID
    Eigen::VectorXd dxDes;    // 期望增量
    Eigen::VectorXd ddxDes;   // 期望加速度
    Eigen::MatrixXd J;        // 任务雅可比
    Eigen::MatrixXd N;        // 零空间投影矩阵
    Eigen::MatrixXd kp, kd;   // 增益矩阵
    Eigen::VectorXd errX;     // 位置误差
};
```

**核心方法**:

| 方法 | 功能 |
|------|------|
| `addTask(const char*)` | 添加新任务 |
| `buildPriority(vector<string>&)` | 构建优先级链 |
| `computeAll(...)` | 计算所有任务 |

---

## 8. 控制执行层 (Execution)

### 8.1 PVT_Ctr - 关节PVT控制

**文件位置**: `common/PVT_ctrl.h` / `PVT_ctrl.cpp`

**功能职责**:
- 关节位置-速度-力矩（PVT）闭环控制
- 支持关节限位保护
- 支持PD控制模式
- 低通滤波输出力矩

**控制模式**:
1. **PVT模式**: 位置-速度-力矩三闭环
2. **PV模式**: 仅位置-速度闭环
3. **PD模式**: 仅位置闭环（力矩控制）

**核心方法**:

| 方法 | 功能 |
|------|------|
| `calMotorsPVT()` | 计算PVT控制 |
| `calMotorsPVT(double deltaP_Lim)` | 带位置限制的PVT |
| `setJointPD(double kp, double kd, string)` | 设置关节PD参数 |
| `dataBusRead(DataBus&)` | 读取数据 |
| `dataBusWrite(DataBus&)` | 写入数据 |

**关节PD控制**:
```cpp
tau_out = kp * (pos_des - pos_cur) + kd * (vel_des - vel_cur);
```

**依赖关系**:
```
PVT_Ctr → LPF_Fst → DataBus
                        │
                        ├──读取: RobotState.motors_pos_des, RobotState.motors_vel_des
                        │
                        └──写入: RobotState.motors_tor_out
```

---

## 9. 数学工具层 (Math Utilities)

### 9.1 模块列表

| 模块 | 文件 | 功能 |
|------|------|------|
| LPF_Fst | `math/LPF_fst.h` | 一阶低通滤波器 |
| Bezier_1D | `math/bezier_1D.h` | 一维贝塞尔曲线 |
| RampTrajectory | `math/ramp_trajectory.h` | 斜坡轨迹生成 |
| useful_math | `math/useful_math.h` | 常用数学工具（旋转矩阵、四元数等） |

### 9.2 LPF_Fst - 低通滤波器

**传递函数**:
```
G(s) = 1 / (Ts + 1)
```

**离散化**:
```
y(k) = alpha * x(k) + (1-alpha) * y(k-1)
alpha = dt / (T + dt)
```

### 9.3 RampTrajectory - 斜坡轨迹

**功能**: 生成平滑的速度/位置斜坡轨迹

**公式**:
```
v(t) = v_des * (1 - exp(-t/tau))
p(t) = p_ini + integral(v(t) dt)
```

---

## 10. Demo程序架构

### 10.1 walk_mpc_wbc.cpp - MPC+WBC行走

**完整控制流程**:

```cpp
// 1. 仿真接口层
mj_interface.updateSensorValues();           // 读取传感器
mj_interface.dataBusWrite(RobotState);        // 写入DataBus

// 2. 运动学/动力学计算
kinDynSolver.dataBusRead(RobotState);         // 读取状态
kinDynSolver.computeJ_dJ();                   // 计算雅可比
kinDynSolver.computeDyn();                    // 计算动力学
kinDynSolver.dataBusWrite(RobotState);        // 写回DataBus

// 3. 指令解析（摇杆）
jsInterp.step();                              // 计算期望速度
jsInterp.dataBusWrite(RobotState);

// 4. 步态调度
gaitScheduler.dataBusRead(RobotState);
gaitScheduler.step();
gaitScheduler.dataBusWrite(RobotState);

// 5. 落脚点规划
footPlacement.dataBusRead(RobotState);
footPlacement.getSwingPos();
footPlacement.dataBusWrite(RobotState);

// 6. MPC计算 (200Hz)
MPC_solv.dataBusRead(RobotState);
MPC_solv.cal();
MPC_solv.dataBusWrite(RobotState);

// 7. WBC计算 (1kHz)
WBC_solv.dataBusRead(RobotState);
WBC_solv.computeDdq(kinDynSolver);
WBC_solv.computeTau();
WBC_solv.dataBusWrite(RobotState);

// 8. 关节PVT控制
pvtCtr.dataBusRead(RobotState);
pvtCtr.calMotorsPVT();
pvtCtr.dataBusWrite(RobotState);

// 9. 输出到仿真器
mj_interface.setMotorsTorque(RobotState.motors_tor_out);

// 10. 数据记录
logger.recItermData(...);
```

---

## 11. 接口依赖深度总结

### 11.1 依赖深度层级（从前到后）

| 层级 | 模块 | 深度 | 说明 |
|------|------|------|------|
| 1 | 用户输入/GLFW | L0 | 最前端，外部输入 |
| 2 | MJ_Interface | L1 | 直接与Mujoco交互 |
| 3 | DataBus | L2 | 数据中枢 |
| 4 | JoyStickInterpreter | L3 | 指令解析 |
| 5 | GaitScheduler | L3 | 步态规划 |
| 6 | FootPlacement | L3 | 落脚点规划 |
| 7 | MPC | L4 | 模型预测控制 |
| 8 | Pin_KinDyn | L4 | 运动学/动力学 |
| 9 | WBC_priority | L5 | 全身控制 |
| 10 | PriorityTasks | L6 | 任务管理 |
| 11 | PVT_Ctr | L5 | 关节控制 |
| 12 | Mujoco | L6 | 物理仿真 |

### 11.2 数据流总结

```
用户输入 ──→ JoyStickInterpreter ──┐
                                  │
Mujoco ──→ MJ_Interface ──→ DataBus ──→ Pin_KinDyn ──┐
                                  │                   │
                                  ├──→ GaitScheduler ──┤
                                  │                   │
                                  ├──→ FootPlacement ──┤
                                  │                   │
                                  └──→ MPC ────────────┼──→ WBC_priority ──→ PVT_Ctr ──→ MJ_Interface ──→ Mujoco
                                                          │
                                              PriorityTasks ←─┘
```

---

## 12. 关键配置参数

### 12.1 MPC参数

| 参数 | 说明 | 典型值 |
|------|------|--------|
| `dt_200Hz` | MPC控制周期 | 0.005s |
| `mpc_N` | 预测步数 | 10 |
| `u_weight` | 控制输入权重 | 1e-6 |
| `L_diag(0-2)` | 姿态误差权重 | 1.0 |
| `L_diag(3-5)` | 位置误差权重 | 1.0~200.0 |
| `L_diag(6-8)` | 角速度误差权重 | 1e-7 |
| `L_diag(9-11)` | 线速度误差权重 | 10.0~100.0 |

### 12.2 WBC参数

| 参数 | 说明 | 典型值 |
|------|------|--------|
| `Q1` | 接触力误差权重 | 对角矩阵 |
| `Q2` | 关节加速度权重 | 对角矩阵 |
| `nWSR` | QP迭代上限 | 100 |

### 12.3 步态参数

| 参数 | 说明 | 典型值 |
|------|------|--------|
| `tSwing` | 摆动时间 | 0.4s |
| `stepHeight` | 抬腿高度 | 0.1~0.15m |
| `kp_vx` | x方向落脚点增益 | 0.03 |
| `kp_vy` | y方向落脚点增益 | 0.03 |
| `kp_wz` | 转向落脚点增益 | 0.03 |
| `FzThrehold` | 触地力阈值 | 100N |

### 12.4 关节PD参数

| 关节 | Kp | Kd |
|------|-----|-----|
| ankle_pitch | 100 | 10 |
| ankle_roll | 100 | 10 |
| knee_pitch | 1000 | 100 |

---

## 13. 文件清单

### 13.1 核心头文件

| 路径 | 说明 |
|------|------|
| `sim_interface/MJ_interface.h` | Mujoco接口 |
| `sim_interface/GLFW_callbacks.h` | GLFW回调 |
| `common/data_bus.h` | 数据总线 |
| `common/PVT_ctrl.h` | PVT控制 |
| `common/data_logger.h` | 数据记录 |
| `algorithm/mpc.h` | MPC控制器 |
| `algorithm/wbc_priority.h` | WBC控制器 |
| `algorithm/pino_kin_dyn.h` | 运动学/动力学 |
| `algorithm/gait_scheduler.h` | 步态调度 |
| `algorithm/foot_placement.h` | 落脚点规划 |
| `algorithm/joystick_interpreter.h` | 摇杆解析 |
| `algorithm/priority_tasks.h` | 优先级任务 |
| `math/LPF_fst.h` | 低通滤波器 |
| `math/ramp_trajectory.h` | 斜坡轨迹 |
| `math/bezier_1D.h` | 贝塞尔曲线 |
| `math/useful_math.h` | 数学工具 |

### 13.2 Demo程序

| 文件 | 说明 |
|------|------|
| `demo/walk_mpc_wbc.cpp` | MPC+WBC行走 |
| `demo/walk_wbc.cpp` | 纯WBC行走 |
| `demo/jump_mpc.cpp` | MPC跳跃 |
| `demo/float_control.cpp` | 浮空控制 |
| `demo/walk_wbc_joystick.cpp` | WBC+摇杆行走 |
| `demo/walk_mpc_wbc_joystick.cpp` | MPC+WBC+摇杆 |
| `demo/walk_wbc_staircase.cpp` | WBC楼梯行走 |

---

## 14. 第三方库依赖

| 库 | 用途 | 版本 |
|-----|------|------|
| Mujoco | 物理仿真引擎 | 3.1.1 |
| Pinocchio | 运动学/动力学 | - |
| Eigen | 线性代数 | 3.x |
| qpOASES | QP求解器 | 3.2 |
| GLFW | 图形界面 | 3.3 |
| jsoncpp | JSON解析 | - |
| quill | 日志记录 | - |
| Boost | 辅助工具 | - |

---

## 15. 命名规范

### 15.1 后缀说明

| 后缀 | 含义 |
|------|------|
| `_L` | 本体坐标系 (Local) |
| `_W` | 世界坐标系 (World) |
| `_des` | 期望值 (Desired) |
| `_cur` | 当前值 (Current) |
| `_est` | 估计值 (Estimated) |
| `fe_` | 足末端 (Foot End) |
| `hd_` | 手部 (Hand) |

### 15.2 腿状态

| 标识 | 含义 |
|------|------|
| `sw`, `swing` | 摆动腿 |
| `st`, `stance` | 支撑腿 |
| `L`, `_l` | 左侧 |
| `R`, `_r` | 右侧 |

---

*文档版本: 1.0*
*最后更新: 2024*
